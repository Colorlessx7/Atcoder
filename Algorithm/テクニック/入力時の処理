int n, k, q;
    cin >> n >> k >> q;
    vector<int> p(n,k - q);
    rep(i,q){
        int a;
        cin >> a;
        a--;
        p[a]++;
    }
 
 ABC141Cから
 このようにvector a[q] を宣言したところで処理に影響はないので
 入力時に処理させてしまってもいい
